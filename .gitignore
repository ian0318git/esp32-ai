# python
__pycache__/
.venv/
*.pyc

# data (regenerate with data/prepare.py) — keep tokenizer json
data/*.bin
data/*.txt
data/tinystories_slice.txt

# training checkpoints (large, 111MB — too big for git)
runs/

# compiled build artifacts
firmware/esp32_llm/build/

# python egg info
src/*.egg-info/
*.egg-info/

# raw video / recordings (demo footage — keep out of the repo)
video/
videos/
*.mp4
*.mov
*.MOV
*.MP4
firmware/*/build/
